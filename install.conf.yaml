- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc: vimrc
    ~/.config/nvim/init.vim: init.vim
    ~/.bashrc: bashrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
